[Unit]
Description=Cron Daemon
After=syslog.target

[Service]
Type=forking
EnvironmentFile=/etc/default/dcron
ExecStart=/usr/sbin/crond $EXTRA_OPTS
ExecReload=/bin/kill -HUP $MAINPID
PIDFile=/run/crond.pid
Restart=on-failure
KillMode=process

[Install]
WantedBy=multi-user.target
